{
  "name": "@orbit/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for Orbit ERP",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf node_modules .turbo dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
