# Copie este arquivo para `.env` e preencha com as credenciais do Supabase.
# Caso prefira usar outro banco suportado pelo SQLAlchemy basta definir ORBIT_DATABASE_URL.

# Referência do projeto (disponível no painel do Supabase)
ORBIT_SUPABASE_PROJECT_REF=xxxxxxxxxxxxxxxxxxxx

# Credenciais do Postgres gerenciadas pelo Supabase
ORBIT_SUPABASE_DB_PASSWORD=your-postgres-password
# Opcionalmente personalize usuário, base e porta
# ORBIT_SUPABASE_DB_USER=postgres
# ORBIT_SUPABASE_DB_NAME=postgres
# ORBIT_SUPABASE_DB_PORT=5432

# URL pública do projeto e chave service role caso a API precise acessar outros recursos do Supabase
ORBIT_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
ORBIT_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Alternativa: informe diretamente a URL de conexão SQLAlchemy
# ORBIT_DATABASE_URL=postgresql+psycopg2://postgres:password@db.xxxxxxxxxxxx.supabase.co:5432/postgres?sslmode=require
